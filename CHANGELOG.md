# Changelog

## 0.1.0rc1

- 建立独立公开预览基线。
- 移除个人服务默认值，要求显式模型服务 URL。
- Preview 不再显示 API Key 前缀或写入持久 OpenCode 报告。
- OpenCode 写入要求显式 `--replace-providers`。
- 构建改为使用显式、可审计的固定 catalog 输入。
- 将安全回归测试纳入项目测试目录。
